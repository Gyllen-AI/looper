Handling anything that would cost somebody if it got out: the roof over
`secure/secrets` (the credential itself), `secure/identity` (who may do what) and
`secure/input` (what arrives from outside). Pull the one you are in.

- **A secret has one shape: fetched where it is used, never copied, never
  printed.** One in a log is an incident, not a bug.
