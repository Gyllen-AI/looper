Committing here, and keeping the design record straight.

- `docs/PLAN.md` is the design record, written before the code and corrected in
  place as the code corrects it, in the same commit as the work. A reversed
  decision keeps both halves, or the same argument returns in a month.
- Every claim about what looper does is covered by a test, or stated plainly as
  not yet built. `tests/plan-is-true.test.ts` holds the rule table to that.
